import { GetCryptocurrencies } from './get-cryptocurrencies';

describe('GetCryptocurrencies', () => {
  it('should be defined', () => {
    expect(new GetCryptocurrencies()).toBeDefined();
  });
});
