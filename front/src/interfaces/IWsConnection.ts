export interface IWsConnection {
    _coin: string[],
    coinId: string,
    _o: number,
    e: string,
    E: string,
    s: string,
    c: string,
    o: string,
    h: string,
    l: string,
    v: string,
    q: string
}

export interface IminiTicker {
    e: string, E: number, s: string, c: number, o: number, h: number, l: number, v: number, q: number
}